<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Rumbl!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="row">
      <header class="header col-md-12">
        <div class="row">
          <div class="logo-container col-md-6">
            <a href="<%= page_path(@conn, :index) %>" class="logo">
            </a>
          </div>
          <div class="nav-container col-md-6">
            <ul class="list-inline text-right">
              <%= if @current_user do %>
                <li>
                  <%= link @current_user.username, to: page_path(@conn, :index), class: "btn btn-default" %>
                </li>
                <li>
                  <%= link "My Feeds", to: feed_path(@conn, :index), class: "btn btn-info" %>
                </li>
                <li>
                  <%= link "My Videos", to: video_path(@conn, :index), class: "btn btn-success" %>
                </li>
                <li>
                  <%= link "Log out", to: session_path(@conn, :delete, @current_user), method: "delete", class: "btn btn-danger" %>
                </li>
              <% else %>
                <li><%= link "Register", to: user_path(@conn, :new) %></li>
                <li><%= link "Log In", to: session_path(@conn, :new) %></li>
              <% end %>
            </ol>
          </div>
        </div>
      </header>
    </div>

    <div class="row">
      <div class="col-md-12">
        <%= if get_flash(@conn, :info) do %>
          <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
        <% end %>
        <%= if get_flash(@conn, :error) do %>
          <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
        <% end %>
        <main role="main">
          <%= render @view_module, @view_template, assigns %>
        </main>
      </div>      
    </div>

    <script>window.userToken = "<%= assigns[:user_token] %>"</script>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
    <script>

    </script>
  </body>
</html>
